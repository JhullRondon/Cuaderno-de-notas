var nombre = 'Jhull', edad = 29

function printAge(n, e) {
   console.log(`${n} tiene ${e} años`)
}

// printAge('Melissa', 25)

function printName () {
   nombre = nombre.toUpperCase()
   console.log(nombre)
}